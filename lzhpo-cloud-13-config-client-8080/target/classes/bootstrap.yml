spring:
  cloud:
    config:
      #github上的配置名称，注意没有yml后缀名
      name: lzhpo-config-application
      #本次访问的环境配置项
      profile: prod
      #远程库的分支名
      label: master
      #Config配置中心地址，通过它获取lzhpo-config-application.yml信息
      uri: http://localhost:5001